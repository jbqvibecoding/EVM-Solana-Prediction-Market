{
  "name": "matching-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain order matching engine for the Solana prediction-market CLOB (settles via the on-chain exchange program).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@solana/web3.js": "^1.95.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tweetnacl": "^1.0.3",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
